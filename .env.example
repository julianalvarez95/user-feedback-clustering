# Required — used for both embeddings (text-embedding-3-small) and labeling (gpt-4o-mini)
OPENAI_API_KEY=sk-...

# Optional — override embedding cache location (default: .embeddings_cache.json)
# EMBEDDINGS_CACHE_PATH=.embeddings_cache.json
